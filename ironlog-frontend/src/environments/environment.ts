export const environment = {
  /**
   * URL del backend Spring Boot.
   *
   * In locale il backend gira su http://localhost:8080.
   * Per provare l'app dal telefono sulla stessa rete Wi-Fi, sostituisci
   * "localhost" con l'indirizzo IP della tua macchina (es. http://192.168.1.10:8080)
   * e assicurati che quell'origine sia ammessa da `cors.allowed-origins`
   * nell'application.properties del backend.
   */
  apiUrl: 'http://localhost:8080'
};
