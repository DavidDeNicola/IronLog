import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.daviddenicola.ironlog',
  appName: 'IronLog',
  webDir: 'dist/ironlog-frontend/browser'
};

export default config;
